DB_URI=mongodb://localhost:27017/realtime-chat  # Replace with your MongoDB connection URI (or use MongoDB Atlas)
JWT_SECRET=your_jwt_secret  # Secret for JWT token generation (use a secure string)
PORT=3000  # Port to run the app (you can change this if needed)
